{
  "name": "obs-streak-tracker",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test test/*.test.js src/**/*.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
