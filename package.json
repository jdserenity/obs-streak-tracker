{
  "name": "obs-streak-tracker",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test test/*.test.js src/**/*.test.js",
    "reconstruct-history": "node scripts/reconstruct-activity-history.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "canvas-confetti": "^1.9.4"
  }
}
